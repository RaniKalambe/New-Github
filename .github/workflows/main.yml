name: Rani
on: 
 push: 
  branches: [main]

jobs:
  build:
  runs-on:  ubuntu-latest

   steps:
    name: checkout code
    uses: actions/checkout@v4

    name: Run a command
    run: echo " hello from apun"
